package anaydis.sort;

import org.jetbrains.annotations.NotNull;

import java.util.Objects;

public interface ObservableSorter{
    void swaps(@NotNull Objects one, Objects Two);
    void Comparaciones(int First, int Second);
}

