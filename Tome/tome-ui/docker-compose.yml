services:
  front:
    container_name: front
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - tome-net
    env_file:
      - .env.docker.local
    ports:
      - '3000:3000'

networks:
  tome-net:
    external: true
    name: tome_network
